export default function Section({ heading, content }) {
    return (
        <>
            <h2>{heading}</h2>
            <div>
                {content}
            </div>
            {/* menu items */}
        </>
        
    )
}